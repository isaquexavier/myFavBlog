<div>
	<h1>My favorite blog!</h1><br>
	<% @post.each do |post| %>
		<div class="well">
			<h3><%= post.title %></h3>
			<p><%= post.body %></p><br>
			<%= link_to "Reade more", post_path(post), :class => 'btn btn-default' %>
		</div>
	<% end %>	
</div>
